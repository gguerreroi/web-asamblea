<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/1.10.25/css/jquery.dataTables.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a81368914c.js"></script>
    <style>
        .modal {
            transition: opacity 0.25s ease;
        }
        body.modal-active {
            overflow-x: hidden;
            overflow-y: visible !important;
        }

    </style>
    <title>Administrador</title>
</head>
<body class="bg-gray-300"
      style="font-family: Roboto;"
      x-data="{ 'showModal': false }"
      @keydown.escape="showModal = false"
      x-cloak>
<%- include('../partials/header.ejs') %>
<br>

<div class="flex w-screen justify-center items-center">
    <div class="bg-white w-5/6 m-4 py-8 px-4 rounded-lg shadow-lg">
        <button class="modal-open float-right text-white bg-blue-600 hover:bg-blue-700 rounded-full h-8 w-8 items-center justify-center focus:outline-none">
            <i class="fas fa-plus"></i>
        </button>
        <br>
        <br>
        <table class="table-auto " id="tbvotaciones" width="100%">
            <caption class="text-2xl font-semibold p-2">Control de Votaciones</caption>
            <thead>
            <tr class="bg-blue-900 text-white">
                <td class="p-2">#</td>
                <td>Titulo</td>
                <td>Descripción</td>
                <td>Fecha/Hora Inicio</td>
                <td>Imagen</td>
                <td>Link</td>
                <td>Fecha/Hora Fin</td>
                <td>Estado</td>
                <td class="">Operaciones</td>
            </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>

<%- include('../partials/modalVotaciones.ejs'); -%>
<%- include('../partials/common.ejs'); -%>
<script
        src="https://code.jquery.com/jquery-3.6.0.js"
        integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk="
        crossorigin="anonymous"></script>
<script src="https://cdn.datatables.net/1.10.25/js/jquery.dataTables.min.js"></script>
<script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
<script>
    let tbvotaciones;

    function editVotacion(codvotacion) {

    }

    function startVotacion(codvotacion) {
        swal({
            title: "Votaciones",
            text: "Esta a punto de iniciar una votacion, esta seguro?",
            icon: 'warning',
            dangerMode: true,
            buttons: {
                cancel: "Cancelar",
                delete: "Iniciar"
            }
        }).then(function(resp){
            if (resp){
                $.ajax({
                    url: `${URL_API}/votaciones/${codvotacion}`,
                    method: 'post'
                }).then(resvo => {
                    console.log(resvo)
                    swal({
                        title: 'Votaciones',
                        text: 'Votacion iniciada con exito',
                        icon: 'success'
                    })
                    tbvotaciones.ajax.reload(null, false)
                })
            }
        })

    }

    function stopVotacion(codvotacion) {
        swal({
            title: "Votaciones",
            text: "Esta a punto de iniciar una votacion, esta seguro?",
            icon: 'warning',
            dangerMode: true,
            buttons: {
                cancel: "Cancelar",
                delete: "Detener"
            }
        }).then(function(resp){
            if (resp){
                $.ajax({
                    url: `${URL_API}/votaciones/${codvotacion}`,
                    method: 'put'
                }).then(resvo => {
                    console.log(resvo)
                    swal({
                        title: 'Votaciones',
                        text: 'Se ha detenido la votacion',
                        icon: 'success'
                    })
                    tbvotaciones.ajax.reload(null, false)
                })
            }
        })
    }

    function showResultados(codvotacion) {

    }

    function saveVotacion(){

    }

    function loadInfo(codvotacion){

    }

    var openmodal = document.querySelectorAll('.modal-open')
    for (var i = 0; i < openmodal.length; i++) {
        openmodal[i].addEventListener('click', function(event){
            event.preventDefault()
            toggleModal()
        })
    }

    const overlay = document.querySelector('.modal-overlay')
    overlay.addEventListener('click', toggleModal)

    var closemodal = document.querySelectorAll('.modal-close')
    for (var i = 0; i < closemodal.length; i++) {
        closemodal[i].addEventListener('click', toggleModal)
    }

    document.onkeydown = function(evt) {
        evt = evt || window.event
        var isEscape = false
        if ("key" in evt) {
            isEscape = (evt.key === "Escape" || evt.key === "Esc")
        } else {
            isEscape = (evt.keyCode === 27)
        }
        if (isEscape && document.body.classList.contains('modal-active')) {
            toggleModal()
        }
    };


    function toggleModal () {
        const body = document.querySelector('body')
        const modal = document.querySelector('.modal')
        modal.classList.toggle('opacity-0')
        modal.classList.toggle('pointer-events-none')
        body.classList.toggle('modal-active')
    }


    $(document).ready(function () {

        tbvotaciones = $('#tbvotaciones').DataTable({
            lengthChange: false,
            oLanguage: {
                sLengthMenu: "Mostrar _MENU_ registros por pagina",
                sZeroRecords: "Cargando informacion",
                sInfo: "Pagina _PAGE_ de _PAGES_ de _MAX_ registros",
                sInfoEmpty: "Sin registros disponibles",
                sInfoFiltered: "(filtrando registros de un total de _MAX_ ordenes)",
                sSearch: "Buscar",
                oPaginate: {
                    sFirst: "Primero",
                    sPrevious: "Anterior",
                    sNext: "Siguiente",
                    sLast: "Ultimo"
                }
            },
            ajax: `${URL_API}/votaciones`,
            columns: [
                {"data": 'CodVotacion'},
                {"data": 'StrTitulo'},
                {"data": 'StrDescripcion'},
                {"data": 'DatFecha'},
                {"data": 'FlgImg'},
                {"data": 'FlgLink'},
                {"data": 'DatFechaHoraFin'},
                {"data": 'CodEstado'},
                {"data": null},
            ],
            columnDefs: [
                {
                    targets: [2, 3, 4, 5, 6],
                    visible: false
                }, {
                    targets: [7],
                    render: function (data, type, row) {
                        let str = '';
                        if (data === 'E')
                            str += `<span class="flex text-center rounded-lg bg-green-400 bg-opacity-50 text-green-900 p-1">En Curso</span>`
                        if (data === 'F')
                            str += `<span class="flex text-center rounded-lg bg-red-400 bg-opacity-50 text-red-900 p-1">Finalizada</span>`
                        if (data === 'B')
                            str += `<span class="flex text-center rounded-lg bg-orange-400 bg-opacity-50 text-orange-900 p-1">Borrador</span>`

                        return str;
                    }
                }, {
                    targets: [8],
                    orderable: false,
                    render: function (data, type, row) {
                        return `<p class="flex">
        <button onclick="editVotacion('${row.CodVotacion}')" class="text-white bg-yellow-600 hover:bg-yellow-700 rounded-full h-8 w-8 items-center justify-center focus:outline-none"><i class="fas fa-pencil-alt"></i></button>
		<button onclick="startVotacion('${row.CodVotacion}')"  class="text-white bg-green-600 hover:bg-green-700 rounded-full h-8 w-8 items-center justify-center focus:outline-none"><i class="fas fa-play"></i></button>
		<button onclick="stopVotacion('${row.CodVotacion}')"  class="text-white bg-red-600 hover:bg-red-700 rounded-full h-8 w-8 items-center justify-center focus:outline-none"><i class="fas fa-stop"></i></button>
		<button onclick="showResultados('${row.CodVotacion}')"  class="text-white bg-blue-600 hover:bg-blue-700 rounded-full h-8 w-8 items-center justify-center focus:outline-none"><i class="fas fa-chart-line"></i></button>
</p>`
                    }
                }
            ]
        })


    })
</script>

</body>
</html>